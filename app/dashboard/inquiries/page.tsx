/**
 * Customer inquiry screen (issue #10 scope: navigation only). Per the Epic #5
 * objective, this route intentionally ships with no body functionality — only the
 * shared layout's navigation. Content is not a "coming soon" placeholder.
 */
export default function InquiriesPage() {
  return <h1 className="text-lg font-semibold text-text-strong">고객 문의</h1>;
}
