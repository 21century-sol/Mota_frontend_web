import { RoutePlaceholder } from "@/components/dashboard/RoutePlaceholder";

// TODO(#14): replace with the vehicle list UI (filters, URL sync).
export default function VehiclesPage() {
  return <RoutePlaceholder title="차량 관리" />;
}
