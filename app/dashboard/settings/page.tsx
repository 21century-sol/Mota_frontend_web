/**
 * Settings screen (issue #10 scope: navigation only). Per the Epic #5 objective, this
 * route is a permanently empty screen — only the shared layout's navigation is
 * provided, no settings functionality is planned for this issue.
 */
export default function SettingsPage() {
  return <h1 className="text-lg font-semibold text-text-strong">설정</h1>;
}
